/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Unity WebGL a besoin de certaines extensions non compressées
  // Junior pourra ajuster ici si besoin
};

module.exports = nextConfig;
